<a class="item" href="/">Chatroom</a>
<a class="item">Messages</a>
<div class="right menu">
  <div class="ui dropdown item">Account<i class="dropdown icon"></i>
    <div class="menu">
      <% if logged_in? %>
        <a <%=link_to "Logout", logout_path, method: :delete, class: "item"%></a>
      <% else %>
        <a class="item" href='login'>Log in</a>
    <% end %>
    </div>
  </div>
  <% if !logged_in? %>
    <div class="item">
    <div class="ui primary button" href='login'>Log in</div>
  <% end %>
  </div>
